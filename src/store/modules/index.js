// 用来管理首页数据的vuex

const state = {
    indexData:{}, //主页数据
}
const mutations = {
    changeIndexData(state,indexData){
        
    }
};

const actions = {
    async getIndexData({commit}){
        
    }
}

export default{
    state,
    mutations,
    actions
}